FROM postgres:15-alpine

# Configurar variáveis de ambiente padrão
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=NgabiDB2024!Secure
ENV POSTGRES_DB=chat_agents

# Expor porta
EXPOSE 5432

# Comando padrão
CMD ["postgres"] 